[user]
    name = Chao DUan
    email = chao0duan@gmail.com

[alias]
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
    summary = log --abbrev-commit --pretty=oneline --author="muteness" --since=last.mon --reverse
    co = checkout
    br = branch
    ci = commit
    st = status


[color]
    ui = auto
    diff = auto
    status = auto
    branch = auto
    interactive = auto
    grep = auto

[diff]
    tool = kdiff3

[merge]
    tool = kdiff3

[core]
    editor = vim

[credential]
    helper = cache --timeout=99999999999
